[project]
name = "multimodal-llms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ollama>=0.6.2",
    "pillow>=12.2.0",
    "transformers>=5.9.0",
]
